{
  "name": "firecrawl-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered web research agent",
  "scripts": {
    "dev": "npm run dev --prefix agent-templates/next",
    "build": "npm run build --prefix agent-templates/next",
    "start": "npm run start --prefix agent-templates/next"
  }
}
