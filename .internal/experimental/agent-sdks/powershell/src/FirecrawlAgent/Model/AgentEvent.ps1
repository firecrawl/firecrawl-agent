#
# Firecrawl Agent API
# AI-powered web research agent built on Firecrawl. Search, scrape, and extract structured data from the web. 
# Version: 0.1.0
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Server-sent event payload (when stream=true)

.PARAMETER Json

JSON object

.OUTPUTS

AgentEvent<PSCustomObject>
#>
function ConvertFrom-JsonToAgentEvent {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        # try to match AgentEventOneOf defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        # try to match AgentEventOneOf1 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf1 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf1"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf1' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        # try to match AgentEventOneOf2 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf2 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf2"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf2' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        # try to match AgentEventOneOf3 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf3 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf3"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf3' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        # try to match AgentEventOneOf4 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf4 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf4"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf4' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        # try to match AgentEventOneOf5 defined in the oneOf schemas
        try {
            $matchInstance = ConvertFrom-JsonToAgentEventOneOf5 $Json

            foreach($property in $matchInstance.PsObject.Properties) {
                if ($null -ne $property.Value) {
                    $matchType = "AgentEventOneOf5"
                    $match++
                    break
                }
            }
        } catch {
            # fail to match the schema defined in oneOf, proceed to the next one
            Write-Debug "Failed to match 'AgentEventOneOf5' defined in oneOf (AgentEvent). Proceeding to the next one if any."
        }

        if ($match -gt 1) {
            throw "Error! The JSON payload matches more than one type defined in oneOf schemas ([AgentEventOneOf, AgentEventOneOf1, AgentEventOneOf2, AgentEventOneOf3, AgentEventOneOf4, AgentEventOneOf5]). JSON Payload: $($Json)"
        } elseif ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "OneOfSchemas" = @("AgentEventOneOf", "AgentEventOneOf1", "AgentEventOneOf2", "AgentEventOneOf3", "AgentEventOneOf4", "AgentEventOneOf5")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in oneOf schemas ([AgentEventOneOf, AgentEventOneOf1, AgentEventOneOf2, AgentEventOneOf3, AgentEventOneOf4, AgentEventOneOf5]). JSON Payload: $($Json)"
        }
    }
}

