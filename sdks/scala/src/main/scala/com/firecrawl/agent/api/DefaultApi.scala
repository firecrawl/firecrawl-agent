/**
 * Firecrawl Agent API
 * AI-powered web research agent built on Firecrawl. Search, scrape, and extract structured data from the web. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package com.firecrawl.agent.api

import com.firecrawl.agent.model.ErrorResponse
import com.firecrawl.agent.model.RunRequest
import com.firecrawl.agent.model.RunResponse
import com.firecrawl.agent.model.Skill
import com.firecrawl.agent.model.WorkerProgress
import com.firecrawl.agent.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object DefaultApi {
  def apply(baseUrl: String = "http://localhost:3000/api/v1") = new DefaultApi(baseUrl)
}

class DefaultApi(baseUrl: String) {

  /**
   * Poll for progress of currently running parallel workers.
   * 
   * Expected answers:
   *   code 200 : Map[String, WorkerProgress] (Worker progress map)
   */
  def getWorkerProgress(
): Request[Either[ResponseException[String, Exception], Map[String, WorkerProgress]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/workers/progress")
      .contentType("application/json")
      .response(asJson[Map[String, WorkerProgress]])

  /**
   * Returns all discovered skills the agent can use.
   * 
   * Expected answers:
   *   code 200 : Seq[Skill] (List of skills)
   */
  def listSkills(
): Request[Either[ResponseException[String, Exception], Seq[Skill]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/skills")
      .contentType("application/json")
      .response(asJson[Seq[Skill]])

  /**
   * Execute a web research task. Supports both streaming (SSE) and non-streaming responses. This is the single consolidated endpoint that replaces /query, /extract, and /plan. 
   * 
   * Expected answers:
   *   code 200 : RunResponse (Successful response)
   *   code 400 : ErrorResponse (Bad request)
   *   code 500 : ErrorResponse (Server error)
   * 
   * @param runRequest 
   */
  def run(runRequest: RunRequest
): Request[Either[ResponseException[String, Exception], RunResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/run")
      .contentType("application/json")
      .body(runRequest)
      .response(asJson[RunResponse])

}
