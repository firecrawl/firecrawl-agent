# coding: utf-8

"""
    Firecrawl Agent API

    AI-powered web research agent built on Firecrawl. Search, scrape, and extract structured data from the web. 

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from firecrawl_agent.models.model_config import ModelConfig
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class SubAgentConfig(BaseModel):
    """
    SubAgentConfig
    """ # noqa: E501
    id: StrictStr = Field(description="Unique identifier for this sub-agent (used as the tool name suffix).")
    name: StrictStr = Field(description="Human-readable name shown in the tool description.")
    description: StrictStr = Field(description="What this sub-agent does. Included in the tool description for the orchestrator.")
    instructions: Optional[StrictStr] = Field(default=None, description="Custom instructions appended to the sub-agent's system prompt.")
    model: Optional[ModelConfig] = None
    tools: Optional[List[StrictStr]] = Field(default=None, description="Firecrawl tools available to this sub-agent.")
    skills: Optional[List[StrictStr]] = Field(default=None, description="Skills to pre-load for this sub-agent.")
    max_steps: Optional[Annotated[int, Field(le=50, strict=True, ge=1)]] = Field(default=10, description="Maximum steps before the sub-agent stops.", alias="maxSteps")
    __properties: ClassVar[List[str]] = ["id", "name", "description", "instructions", "model", "tools", "skills", "maxSteps"]

    @field_validator('tools')
    def tools_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['search', 'scrape', 'interact', 'map']):
                raise ValueError("each list item must be one of ('search', 'scrape', 'interact', 'map')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SubAgentConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of model
        if self.model:
            _dict['model'] = self.model.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SubAgentConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "description": obj.get("description"),
            "instructions": obj.get("instructions"),
            "model": ModelConfig.from_dict(obj["model"]) if obj.get("model") is not None else None,
            "tools": obj.get("tools"),
            "skills": obj.get("skills"),
            "maxSteps": obj.get("maxSteps") if obj.get("maxSteps") is not None else 10
        })
        return _obj


